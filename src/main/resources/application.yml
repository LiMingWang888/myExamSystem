server:
  port: 80
  servlet:
    context-path: /exam
spring:
  redis:
    host: 192.168.199.129
    port: 6379
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_exam?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT
    username: root
    password: 123
mybatis:
  type-aliases-package: com.wlm.exam.pojo
  mapper-locations: classpath:mapper/*.xml
pagehelper:
  propertyName: propertyValue
  reasonable: false
  defaultCount: true # 分页插件默认参数支持 default-count 形式，自定义扩展的参数，必须大小写一致
